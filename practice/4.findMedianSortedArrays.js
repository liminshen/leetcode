/*
 * @Author: sam.li
 * @Date: 2020-09-14 09:53:47
 * @LastEditors: sam.li
 * @LastEditTime: 2020-09-14 09:58:28
 */
/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number}
 */
var findMedianSortedArrays = function(nums1, nums2) {
    // let sumLen = nums1.length + nums2.length;
    // let sum = (nums1.length >0 ? nums1.reduce((prev, cur, index, array) => { return prev + cur}) : 0) + (nums2.length > 0 ? nums2.reduce((prev, cur, index, array) => { return prev + cur}) : 0);
    // return sum / sumLen
};
// findMedianSortedArrays([],[1,3])